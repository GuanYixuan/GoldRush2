本项目围绕 GoldRush2.0 编程掘金争夺赛展开：两名玩家各控制两个角色，在 `17x17` 网格地图上与 NPC 共同争夺金币，并通过官方评测平台上传模型、发起对局和分析回放日志。
后续实现和自动化探测应优先以本仓库整理出的规则、平台接口和 replay 格式文档为准。

## 关于游戏规则

- 本项目游戏规则统一参考`gamerules/gamerules.md`，其中“官方来源”和“数据推断”均可认为是已确认性质。
- 编写功能时请严格依照游戏规则，不得猜测。若规则文档确有不明确之处需要确认，应该直接指出。

## 关于本地环境/工具

- 开发过程使用`goldrush` conda环境
    - 在开发主线功能时，如果发现环境内没有所需包，请提出安装或使用当前包实现，但不要为了“使用当前包实现”而自行造轮子
- 仓库内通用辅助脚本参考`tools/README.md`；新增或修改项目通用工具时应同步更新该文档。

## 关于策略开发

- 策略开发时用到的官方接口请参考`official_sdk/code`
    - 对于C++策略，优先采用相对include的方式链接官方的`official_sdk/code/game_api.h`

- 进行各类实验时，可以将实验assets放到`temp/`中，此文件夹是gitignore的
    - 后续可能会将部分有价值的内容放入正式repo

## 关于在评测平台上提交策略/获取数据

- 评测平台接口请参考`gamerules/platform.md`
    - 所需的key保存在`.env`文件中的`LOGIN_KEY`，不得泄露

- 不得进行未经允许的对局/策略提交操作
    - 主动发起与其它玩家的对局时，不使用很明确的提交名称，避免泄露策略
    - 进行非策略测试时（如探查评测机系统/平台等），注意300ms时限，避免超时导致测试非预期失败
    - 如果包含提交的实验脚本运行失败，先检查是否真的没有提交成功，再考虑重新发起对局

- 关于从评测平台上下载的日志/对局回放格式，请参考`gamerules/replay.md`
    - 评测平台下载的对局回放都是“基于玩家视角的”，更多例子可参见官方给出的 `official_sdk/data/user`
    - `official_sdk/data/full`中的是一些一般无法得到的“上帝视角”回放

## 文档维护原则
本项目文档一律使用中文，并遵循如下规范：

- 准确优先：文档的首要职责是为未来 agent 提供清晰、正确的引导；如果文档与代码或测试冲突，应尽快修正文档或补测试，不要让失真长期存在。
- 简洁优先：文档应尽量只保留接手任务真正需要的信息，不要把模块页、首页或导航文档写成冗长摘要，避免未来 agent 在入口层消耗过多上下文。
- 角色清楚：新增文档前先判断它是首页、协作导航、活架构文档、模块定位文档，还是研究文档。
- 单点冻结：同一条规则尽量只在一个地方冻结；导航文档不要重复 spec 正文。
- 整体改写优先：修改文档时，从整体定位和一致性出发进行删改，不要只在文末 append，最终导致前后矛盾。
- 后人意识：如果根据某文档进行操作时踩了坑，且该坑后续agent也会踩，则在回答中提示修改文档

## 其它重要仓库目录
- `fast_probing/`：一系列快速的专家系统策略的测试区
- `mechanism/`：利用多对局建模游戏机制的工作文件夹