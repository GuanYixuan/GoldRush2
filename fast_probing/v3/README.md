# fast_probing v3 视野贪心策略

v3 基于 v2 的无 BFS 曼哈顿贪心策略，加入低信息状态下的视野购买。目标是保持 v2 的快速响应，同时减少“金币被炸弹/障碍挡住却持续吸引角色”的空转。

实现仍采用相对 include 引用官方接口：`../../official_sdk/code/game_api.h`。

## 核心思路

- 目标选择只扫描两个己方单位当前视野窗口，选 `grid > 0` 且 `min(md0, md1) <= 6` 的最大金币堆；普通视野扫两个 `5x5`，购买 `vp=2` 后下一回合扫两个 `9x9`，两个窗口允许重叠重复扫描。
- 移动保持 v2：主行动角色逐步向目标贪心移动；每步按方向顺序遇到第一个安全且缩短曼哈顿距离的动作就返回，否则返回第一个安全 fallback。
- 到达金币目标后，只在仍有拾取收益时执行有限次“退出-再进入”。
- 主角色完成必要动作后，剩余步数交给副角色向中心 `(8,8)` 移动，减少单角色长期活跃、另一角色停在角落的问题。
- fallback 保持 v2：没有候选金币时，随机选一个角色向中心 `(8,8)` 贪心移动。
- 新增视野规则：连续 2 回合没有成功抵达 `>1` 的金币目标时，本回合购买一次 `9x9` 视野，即 `vp=2`。

## “可抵达”的折中定义

v3 不恢复 BFS，因此“可抵达”不是严格路径可达。这里采用折中判据：

- 若本回合选中了金币目标，且目标金币数 `>1`，并且当前贪心动作实际走到了目标格，则认为本回合有可抵达高价值金币。
- 否则认为本回合没有可抵达高价值金币。

这会把以下情况计为坏回合：

- 没有曼哈顿 6 步内金币。
- 只有 `1` 金币小堆。
- 选中了 `>1` 金币，但贪心被炸弹、障碍、迷雾、敌人或拥挤格挡住，最终没走到。
- fallback 向中心。

若连续 2 个坏回合出现，则购买一次 `9x9` 视野，并重置计数。

## 状态与费用控制

- 使用静态计数器保存连续坏回合数。
- 使用静态变量保存当前回合视野半径，用于限制金币扫描范围。
- `input->round == 0` 时重置计数和视野半径，避免跨局污染。
- 购买 `vp=2` 后立刻清零。
- 本回合购买 `vp=2` 后，下一回合按 `9x9` 半径扫描；否则下一回合回到默认 `5x5`。
- 第一版不加额外冷却；若实测 `vision_spent` 过高，再考虑买后冷却若干回合。

## 有收益 bounce 阈值

按规则，每次进入金币格拾取当前地面金币的 `ceil(65%)`。精确模拟可得：

- `1..2` 金币：一次进入即可耗尽。
- `3..8` 金币：两次进入即可耗尽。
- `9..25` 金币：三次进入即可耗尽。

单回合最多 6 步，因此当前实现只硬编码到三次进入。若主角色已经通过移动进入过目标，则这次进入已发生，后续额外 bounce 上限为：

- `gold <= 2`：`0` 次。
- `3 <= gold <= 8`：`1` 次。
- `gold >= 9`：`2` 次。

如果角色本回合一开始就在金币格上，因为原地不会拾取，则第一次“退出-再进入”仍可能有收益。

## 速度预期

v3 已采用 single-pass 移动选择、窗口化金币扫描和 step/safe 热路径裁剪。当前 self-play 实测 P90 约 `870-920ns`。

## 实测关注

- P90 是否仍接近 v2。
- `vision_spent` 是否过高。
- 毛金币和扣除视野费用后的净金币是否改善。
- 被炸弹/障碍挡住时是否减少无意义来回移动。

## 平台实测记录

先手比口径：每回合取 `end.dispatch_order` 中第一个正数玩家 id 作为先手，计算 `先手回合数 / 500`。

`game_id=34255` self-play，地图2，`SSTa` vs `SSTb`，在窗口扫描基础上加入 `chooseStepToward` 早停、`crowdedNpcAt` fast path 和初始化裁剪后，无 `forfeit`，回放已解析。结果：`SSTb` 胜，平台记录净金币 `2247 vs 1987`。

| 模型 | 毛金币 | 视野花费 | 视野开销占比 | 净金币 | P50 | P90 | P95 | P99 | Avg | Max |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| SSTa | 2122 | 135 | 6.36% | 1987 | 730ns | 920ns | 990ns | 1.23us | 750ns | 1.61us |
| SSTb | 2343 | 96 | 4.10% | 2247 | 720ns | 870ns | 960ns | 1.12us | 732ns | 1.60us |

相较 `game_id=34202` 的 `1.13us` P90，step/safe 热路径裁剪将 v3 self-play P90 降到约 `870-920ns`。

外战补充：

| game_id | 地图 | 对手 | 结果 | 毛金币 | 视野花费 | 视野开销占比 | 净金币 | P90 | 先手比 |
| ---: | --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 35194 | 地图2 | 君の仿瓷 / player86 | 胜 | 2943 vs 1877 | 99 vs 111 | 3.36% | 2844 vs 1766 | 920ns vs 3.16ms | 100.0% |
| 31175 | 地图2 | 测试用户3 / player4 | 胜 | 2781 vs 1492 | 117 vs 33 | 4.21% | 2664 vs 1459 | 1.31us vs 1.43us | 76.4% |
| 31240 | 地图2 | 做什么都不队 / player111 | 胜 | 2792 vs 1988 | 123 vs 75 | 4.41% | 2669 vs 1913 | 1.31us vs 1.67us | 69.8% |
| 31276 | 地图2 | goooo / player28 | 胜 | 2974 vs 1996 | 132 vs 75 | 4.44% | 2842 vs 1921 | 1.33us vs 1.56us | 71.2% |
| 32590 | 地图1 | 唐人街 / player124 | 胜 | 2099 vs 1605 | 237 vs 78 | 11.29% | 1862 vs 1527 | 1.37us vs 19.77us | 100.0% |
